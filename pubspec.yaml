name: glm50_27cg_complete
description: A full Flutter app for Bosch GLM50-27CG with working Android folder.
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue: ^0.8.0
  path_provider: ^2.0.11
  pdf: ^3.10.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
